import java.util.Arrays;

class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
       /* //方法1：(1)直接将nums2数组中的元素插入nums1
        for (int i = 0; i < n; i++) {
            nums1[m+i] = nums2[i];
        }
        //(2)排序后对整个数组进行排序
        Arrays.sort(nums1);*/
        //

    }


}